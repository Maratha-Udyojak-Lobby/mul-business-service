# MUL Business Service

Business directory, profiles, verification, and ratings.

## Development

```bash
pip install -r requirements.txt
python -m uvicorn app.main:app --reload --port 8002
```

Server runs on http://127.0.0.1:8002
